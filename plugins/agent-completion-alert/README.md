# Agent Completion Alert Plugin

Plays a success sound and shows a bottom in-app toast when an agent message completes.

## Install

Copy this folder to your configured plugin directory, for example:

- `~/.codex/codex-monitor-plugins/agent-completion-alert`

Then open Plugin Manager and refresh plugins.
